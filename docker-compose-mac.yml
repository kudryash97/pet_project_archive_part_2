version: '3.8'

x-airflow-common:
  &airflow-common
  build:
    context: ./docker/airflow
    dockerfile: Dockerfile
  networks:
    - airflow
  environment:
    &airflow-common-env
    AIRFLOW__API__AUTH_BACKENDS: 'airflow.api.auth.backend.basic_auth,airflow.api.auth.backend.session'
    AIRFLOW__CELERY__BROKER_URL: redis://:@af_redis:6379/0
    AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@af_postgres/airflow
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: 'true'
    AIRFLOW__CORE__EXECUTOR: CeleryExecutor
    AIRFLOW__CORE__FERNET_KEY: ''
    AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
    AIRFLOW__CORE__TEST_CONNECTION: Enabled
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@af_postgres/airflow
    AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: 'true'
    AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL: 5
    AIRFLOW__SCHEDULER__MIN_FILE_PROCESS_INTERVAL: 5
    _PIP_ADDITIONAL_REQUIREMENTS: ${_PIP_ADDITIONAL_REQUIREMENTS:-}
    DOCKER_HOST: unix:///var/run/docker.sock
  volumes:
    - ./workspace/dags:/opt/airflow/dags:rw
    - ./workspace/dbt:/opt/airflow/dbt
    - ./workspace/logs:/opt/airflow/logs
    - ./workspace/shared_workers_data:/opt/airflow/shared_workers_data
    - ./docker/airflow/plugins:/opt/airflow/plugins:rw
    - /var/run/docker.sock:/var/run/docker.sock:rw
  user: "${AIRFLOW_UID:-50000}:0"
  group_add:
    - "999"
  # depends_on убран из якоря, чтобы избежать циклов

services:
  af_postgres:
    image: postgres:17
    container_name: af_postgres
    hostname: af_postgres
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - postgres-data:/var/lib/postgresql/data
    mem_limit: 1G
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 5s
      retries: 5
    networks:
      - airflow

  af_redis:
    image: redis:7.2-bookworm
    container_name: af_redis
    hostname: af_redis
    expose:
      - 6379
    mem_limit: 256M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - airflow

  af_init:
    <<: *airflow-common
    container_name: af_init
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db init
        airflow users create \
          --username ${_AIRFLOW_WWW_USER_USERNAME:-airflow} \
          --password ${_AIRFLOW_WWW_USER_PASSWORD:-airflow} \
          --firstname Admin \
          --lastname User \
          --role Admin \
          --email admin@example.com
        echo "Airflow database initialized."
    environment:
      <<: *airflow-common-env
      _AIRFLOW_DB_MIGRATE: 'true'
      _AIRFLOW_WWW_USER_CREATE: 'true'
      _AIRFLOW_WWW_USER_USERNAME: ${_AIRFLOW_WWW_USER_USERNAME:-airflow}
      _AIRFLOW_WWW_USER_PASSWORD: ${_AIRFLOW_WWW_USER_PASSWORD:-airflow}
    user: "0:0"
    depends_on:
      af_postgres:
        condition: service_healthy
      af_redis:
        condition: service_healthy

  af_webserver:
    <<: *airflow-common
    container_name: af_webserver
    command: webserver
    ports:
      - "8080:8080"
    mem_limit: 1G
    cpus: 0.5
    depends_on:
      af_postgres:
        condition: service_healthy
      af_redis:
        condition: service_healthy
      af_init:
        condition: service_completed_successfully

  af_scheduler:
    <<: *airflow-common
    container_name: af_scheduler
    command: scheduler
    mem_limit: 1G
    cpus: 0.5
    depends_on:
      af_postgres:
        condition: service_healthy
      af_redis:
        condition: service_healthy
      af_init:
        condition: service_completed_successfully

  af_worker_spark:
    <<: *airflow-common
    container_name: af_worker_spark
    command: celery worker -q spark_pool
    mem_limit: 3G
    cpus: 2
    depends_on:
      af_postgres:
        condition: service_healthy
      af_redis:
        condition: service_healthy
      af_init:
        condition: service_completed_successfully

  af_worker_default:
    <<: *airflow-common
    container_name: af_worker_default
    command: celery worker -q default
    mem_limit: 2G
    cpus: 1
    depends_on:
      af_postgres:
        condition: service_healthy
      af_redis:
        condition: service_healthy
      af_init:
        condition: service_completed_successfully

  af_flower:
    <<: *airflow-common
    container_name: af_flower
    command: celery flower
    ports:
      - "5555:5555"
    mem_limit: 256M
    depends_on:
      af_redis:
        condition: service_healthy
      af_init:
        condition: service_completed_successfully

  zookeeper:
    image: zookeeper:3.7
    container_name: zookeeper
    hostname: zookeeper
    volumes:
      - ./docker/zookeeper/config/zoo.cfg:/conf/zoo.cfg:ro
    environment:
      ZOO_CONF_DIR: /conf
      ZOO_AUTOPURGE_PURGEINTERVAL: 1
      ZOO_AUTOPURGE_SNAPRETAINCOUNT: 3
    mem_limit: 512M
    networks:
      - airflow
    healthcheck:
      test: ["CMD", "zkServer.sh", "status"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  clickhouse01:
    image: clickhouse/clickhouse-server:25.5-alpine
    container_name: clickhouse01
    hostname: clickhouse01
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: clickhouse
    volumes:
      - ./docker/clickhouse/clickhouse01:/etc/clickhouse-server
      - clickhouse01-data:/var/lib/clickhouse
    mem_limit: 1.5G
    cpus: 1
    depends_on:
      zookeeper:
        condition: service_healthy
    networks:
      - airflow

  clickhouse02:
    image: clickhouse/clickhouse-server:25.5-alpine
    container_name: clickhouse02
    hostname: clickhouse02
    ports:
      - "8124:8123"
    environment:
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: clickhouse
    volumes:
      - ./docker/clickhouse/clickhouse02:/etc/clickhouse-server
      - clickhouse02-data:/var/lib/clickhouse
    mem_limit: 1.5G
    cpus: 1
    depends_on:
      zookeeper:
        condition: service_healthy
    networks:
      - airflow

  clickhouse03:
    image: clickhouse/clickhouse-server:25.5-alpine
    container_name: clickhouse03
    hostname: clickhouse03
    ports:
      - "8125:8123"
    environment:
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: clickhouse
    volumes:
      - ./docker/clickhouse/clickhouse03:/etc/clickhouse-server
      - clickhouse03-data:/var/lib/clickhouse
    mem_limit: 1.5G
    cpus: 1
    depends_on:
      zookeeper:
        condition: service_healthy
    networks:
      - airflow

  clickhouse04:
    image: clickhouse/clickhouse-server:25.5-alpine
    container_name: clickhouse04
    hostname: clickhouse04
    ports:
      - "8126:8123"
    environment:
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: clickhouse
    volumes:
      - ./docker/clickhouse/clickhouse04:/etc/clickhouse-server
      - clickhouse04-data:/var/lib/clickhouse
    mem_limit: 1.5G
    cpus: 1
    depends_on:
      zookeeper:
        condition: service_healthy
    networks:
      - airflow

  superset:
    build: ./docker/bi-superset
    container_name: superset
    hostname: superset
    ports:
      - "8088:8088"
    environment:
      SUPERSET_SECRET_KEY: your_secret_key
      DATABASE_HOST: af_postgres
      DATABASE_PORT: 5432
      DATABASE_USER: airflow
      DATABASE_PASSWORD: airflow
      DATABASE_DB: airflow
    depends_on:
      af_postgres:
        condition: service_healthy
    mem_limit: 1G
    cpus: 0.5
    networks:
      - airflow

  jupyter:
    build: ./docker/jupyter
    container_name: jupyter
    hostname: jupyter
    ports:
      - "8888:8888"
      - "4040:4040"
    environment:
      SPARK_MASTER_URL: spark://192.168.0.4:7077   # замените на реальный IP
    volumes:
      - ./workspace/notebooks:/notebooks
    mem_limit: 1.5G
    cpus: 1
    depends_on:
      - clickhouse01
      - clickhouse02
    networks:
      - airflow

  dozzle:
    image: amir20/dozzle:v8.13
    container_name: monitoring
    hostname: monitoring
    ports:
      - "8880:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    mem_limit: 256M
    networks:
      - airflow

volumes:
  postgres-data:
  clickhouse01-data:
  clickhouse02-data:
  clickhouse03-data:
  clickhouse04-data:

networks:
  airflow:
    driver: bridge